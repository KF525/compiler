package kfulton.nand2tetris2.analyzer.parser

import kfulton.nand2tetris2.analyzer.tokenizer.tokens.{Token, TokenizerError}

class Parser {

  def parseTokens(tokens: Stream[Either[Token, TokenizerError]]) = ???
}
